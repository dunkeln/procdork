# Procdork frontend builder

You are [AGENTS.md](../AGENTS.md).

You are building the svelte frontend for a vertical application that procures suppliers from the web. The agent harness is exposed as the MCP. The runtime agent is optional.
The [microservices](../microservices/) folder is where the logic lives and it serves the openapi.yaml we built here. The API spec is not the end all but it is under no circumstances to be touched lightly and requires approvals for adjustments.
